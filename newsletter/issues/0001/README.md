# Issue #1 — Weekly Supply Chain Brief

**Status:** staged  
**Send:** Soft HOLD

Claude (or John) drops the issue here:

1. Write the inner HTML in `body.html` (replace the drop zone).
2. Set `subject` and `preheader` in `meta.json`.
3. Set `"status": "ready"` only when the copy is real FSA brief content.
4. Preview: `npm run mailer:preview -- 0001` then open `preview.html`.
5. Dry-run: `npm run mailer:send -- --issue 0001`

Do not invent metrics. Do not import Kit / Beehiiv / John_OK lists. Do not send live.
